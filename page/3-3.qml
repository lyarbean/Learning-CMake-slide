import QtQuick 2.2
import "../qml"
import "../theme"

Slide {
    headline: "CMake, the right way"
    subHeadline: "Feature"

    // * Speed
    // * Just works wherever a modern c++ compiler lives
    // * Can work with gmake, ninja, Xcode and VisualStudio
    // * Can work with C, C++, Object C/C++, Fortran, and even more
    // * Just a macro Languages with simple syntax and function support
    // * Better denpendencies resolution, and Graphviz support
    // * Better finders
    // * Customizable

}
